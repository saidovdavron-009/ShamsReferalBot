# Certificate template

Drop your certificate background image here as `certificate-template.png` (recommended size: 1200x850px).

If this file doesn't exist, the bot generates a default gold-bordered certificate design automatically — no setup required.

The name/signature text is overlaid by `src/features/certificate/certificate.service.ts` on top of whichever background is used.
